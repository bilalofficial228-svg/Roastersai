kind = "web"
previewPath = "/"
title = "Roastify"
version = "1.0.0"
id = "artifacts/roastify"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25298

[services.development]
run = "pnpm --filter @workspace/roastify run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/roastify", "run", "build" ]
publicDir = "artifacts/roastify/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25298"
BASE_PATH = "/"
